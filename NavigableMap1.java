public interface NavigableMap1 {
    /*
    It is Child Interface of Sorted Map and it Defines Sevral Methods
    -floorKey()-Returns Highest Element <=e
    -lowerKey()-Returns Highest Element <e
    -cellingKey()-Returns Lowest Element >=e
    -higherKey()-Returns Lowest Element >e
    -pollFirstKey()-Returns And Remove First Entry
    -pollLastKey()-Returns And Remove Last Entry
    -descendingMap()-Retuns NavigableMap in Reverse Order
     */
}
